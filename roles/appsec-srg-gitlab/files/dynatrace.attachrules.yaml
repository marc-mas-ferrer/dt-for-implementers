---
attachRules:
  tagRule:
  - meTypes:
    - PROCESS_GROUP_INSTANCE
    tags:
    - context: ENVIRONMENT
      key: DT_RELEASE_PRODUCT
      value: ${DT_RELEASE_PRODUCT}
    - context: ENVIRONMENT
      key: DT_RELEASE_STAGE
      value: ${DT_RELEASE_STAGE}
    - context: ENVIRONMENT
      key: DT_RELEASE_BUILD_VERSION
      value: ${DT_RELEASE_BUILD_VERSION}
